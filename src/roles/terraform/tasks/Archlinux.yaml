---
- name: "TFENV | Install"
  ansible.builtin.shell: "yay -S --needed --noconfirm tfenv"

- name: "TFENV | Add to group"
  become: true
  ansible.builtin.user:
    append: true
    groups: tfenv
    name: "{{ ansible_facts.user_id }}"