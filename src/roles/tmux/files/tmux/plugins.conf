
# TPM — plugin manager (must be declared before other plugins)
set -g @plugin 'tmux-plugins/tpm'

# sensible — opinionated base settings
set -g @plugin 'tmux-plugins/tmux-sensible'

# floax — floating terminal pane overlay
set -g @plugin 'omerxx/tmux-floax'
set -g @floax-border-color '#b16286'
