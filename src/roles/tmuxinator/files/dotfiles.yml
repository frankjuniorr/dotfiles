name: dotfiles
root: /home/frank/Dropbox/code/01.github/projects/dotfiles

windows:
  - code:
      layout: main-horizontal
      panes:
        - clear && figlet -k -c -t "Dotfiles Repo" && onefetch && sleep 1 && code .
        - cd ~/dotfiles && clear && figlet -k -c -t "Dotfiles User" && pwd
  - docker:
    - lazydocker
  - git:
    - lazygit