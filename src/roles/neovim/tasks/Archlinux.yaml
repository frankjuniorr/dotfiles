---
- name: "NEOVIM | Install"
  become: true
  community.general.pacman:
    name: neovim
    state: present
  when: not (omarchy_mode | default(false))
  # Omarchy ships neovim + LazyVim as omarchy-nvim — patched, not replaced, in main.yaml.